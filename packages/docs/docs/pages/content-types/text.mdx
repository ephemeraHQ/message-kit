# Text

The `message` object contains the following

```tsx
{
  id: string, // The id of the message
  content: string, // The content of the message
  senderAddress: string, // The address of the sender
  sent: Date, // The timestamp of the message
  contentType: ContentTypeId; // The content type of the message
}
```

### Receive a message

Here's an example of how you can receive a message in your app:

```tsx
const { content, contentType, senderAddress } = context.message;
const { typeId } = contentType;

if (typeId == "text") {
  // Use content
}
```
