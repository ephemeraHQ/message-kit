# Admin

This example shows how to create an admin bot that will allow you to block and unblock users.

## Commands

Here are the commands to block and unblock users:

```bash [cmd]
/block @alice
/unblock @alice
```

::::note[Commands]
_See how commands are defined in [`src/commands.ts`](https://github.com/xmtp-labs/botkit/blob/main/examples/group/src/commands.ts)_
:::details[Commands]

```json
{
  "name": "Admin",
  "icon": "🔐",
  "description": "Moderate access to the group with admin commands.",
  "commands": [
    {
      "command": "/block",
      "description": "Block a user.",
      "params": {
        "username": {
          "default": "",
          "type": "username"
        }
      }
    },
    {
      "command": "/unblock",
      "description": "Unblock a user.",
      "params": {
        "username": {
          "default": "",
          "type": "username"
        }
      }
    }
  ]
}
```

:::
::::

## Logic

```jsx [src/handler/admin.ts]
import { HandlerContext } from "@xmtp/mkit";

export async function handler(context: HandlerContext) {
  const { content } = context.message;
  const { content: text, command, params } = content;
  switch (command) {
    case "block":
      // context.addMember(params.username);
      context.reply("❌ you are not an admin");
      break;
    case "unblock":
      // context.removeMember(params.username);
      context.reply("❌ you are not an admin");
      break;
  }
}
```
