# Resend middleware

Use Resend to send emails.

## Install

Install dependencies

```bash [cmd]
bun add resend
```

## Example

Send an email with a list of TODOs.

```tsx [src/handlers/todo.ts]
// [!include ~/../../templates/agent/src/handlers/todo.ts]
```
