# `.cursorrules`

Cursor rules file is a predefined set of rules that will allow you to write code better and faster in Cursor. It comes included with the Message Kit template through the CLI.

```tsx [.cursorrules]
// [!include ~/../../.cursorrules]
```
