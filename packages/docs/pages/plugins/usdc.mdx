## USDC

Install dependencies

```bash
bun add ethers
```

USDC is a middleware that allows you to interact with the USDC token.

```tsx [src/lib/usdc.ts]
// [!include ~/../../templates/community/src/lib/usdc.ts]
```
