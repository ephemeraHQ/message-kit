# GM Agent

A bot that replies GM to any message.

## Structure

```
gm/
├── src/
│   ├── index.ts
├── package.json
├── tsconfig.json
└── .env
```

## Runner

The process that starts listening to messages and replying to them.

```tsx [src/index.ts]
// [!include ~/../../templates/gm/src/index.ts]
```

## Variables

Set up these variables in your app

```bash [cmd]
// [!include ~/../../templates/gm/.env.example]
```
