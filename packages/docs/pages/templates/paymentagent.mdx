import Video from "../../components/Video";

# Payment Agent

`paymentagent.eth` is a template that has an agent that can perform transactions on behalf of the user. Since it's a ens domain its reachable from any app that supports ENS.

### Converse

<Video src="/videos/concierge.mp4" />

### Coinbase

<Video src="/videos/cb.mp4" />

### Warpcast

<Video src="/videos/warpcast.mp4" />

## Structure

```
paymentagent/
├── src/
│   └── vibes/
│       ├── degen.ts
│   ├── index.ts
│   └── prompt.ts
├── package.json
├── tsconfig.json
└── .env
```

## Agent

The process that starts listening to messages and replying to them.

```tsx [src/index.ts]
// [!include ~/../../templates/paymentagent/src/index.ts]
```

## Used

- [Concierge](/skills/concierge): Agent Wallet as a Service
- [CDP Wallet Service](/plugins/cdp): Wallet for Coinbase
- [Degen Vibe](/vibes/degen): Define the personality of your agent

## Variables

Set up these variables in your app

```bash [cmd]
// [!include ~/../../templates/paymentagent/.env.example]
```

## End result

The end result is a prompt that the model can understand and use to respond to the user.

```md [example_prompt.md]
// [!include ~/../../templates/paymentagent/example_prompt.md]
```
