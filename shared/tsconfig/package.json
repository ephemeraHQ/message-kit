{
  "name": "@xmtp/tsconfig",
  "version": "0.0.0",
  "private": true,
  "files": [
    "base.json"
  ],
  "scripts": {
    "format": "yarn format:base -w .",
    "format:base": "prettier --ignore-path ../../.gitignore",
    "format:check": "yarn format:base -c ."
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  }
}
