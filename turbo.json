{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["**/.env.*local"],
  "pipeline": {
    "prebuild": {
      "outputs": []
    },
    "build": {
      "dependsOn": ["^build", "prebuild"],
      "outputs": ["dist/**"]
    },
    "clean": {
      "outputs": []
    },
    "format": {
      "outputs": []
    },
    "format:check": {
      "outputs": []
    },
    "test": {
      "outputs": []
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "outputs": []
    }
  }
}
