{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "clean": {
      "outputs": []
    },
    "format": {
      "outputs": []
    },
    "format:check": {
      "outputs": []
    },
    "botkit-docs#dev": {
      "persistent": true
    },
    "group-bot#start": {
      "dependsOn": ["^build", "subscription-bot#build"],
      "persistent": true
    },
    "subscription-bot#start": {
      "dependsOn": ["^build", "subscription-bot#build"],
      "persistent": true
    },
    "gm-bot#start": {
      "dependsOn": ["^build", "gm-bot#build"],
      "persistent": true
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "outputs": []
    }
  }
}
